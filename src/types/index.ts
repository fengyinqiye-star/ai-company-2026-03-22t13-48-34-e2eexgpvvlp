export type NavLink = {
  label: string;
  href: string;
};

export type Feature = {
  icon: string;
  title: string;
  description: string;
};

export type HeroContent = {
  headline: string;
  subtext: string;
  ctaText: string;
  ctaHref: string;
};

export type ContactContent = {
  heading: string;
  description: string;
  email: string;
  phone: string;
  ctaText: string;
};

export type SiteMetadata = {
  siteName: string;
  title: string;
  description: string;
  ogTitle: string;
  ogDescription: string;
};
